{
  "name": "contato",
  "version": "1.0.0",
  "description": "pagina de contato com formulario",
  "main": "index.js",
  "dependencies": {
    "bootstrap": "^4.0.0-beta2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
